def count_word_occurrences(text, word):
    """
    Функция считает количество вхождений слова в текст.
    
    Параметры:
    text (str): Текст для анализа.
    word (str): Слово, количество вхождений которого необходимо найти.
    
    Возвращает:
    int: Количество вхождений слова в текст.
    """
    # Приводим текст к нижнему регистру и разбиваем на слова
    words = text.lower().split()
    word = word.lower()  # Слово тоже приводим к нижнему регистру для корректного сопоставления
    
    # Считаем количество вхождений
    count = words.count(word)
    
    return count

# Пример текста и слова
text_example = "Это пример текста, в котором слово 'пример' встречается несколько раз. Пример."
word_example = "пример"

# Вызываем функцию и печатаем результат
print("Слово '{}' встречается в тексте {} раз(а).".format(word_example, count_word_occurrences(text_example, word_example)))
